<%# app/views/articles/bookmarked.html.erb %>
<% content_for :title, "Surviving Montreal: Your Bookmarked Articles" %>
  <div class="container">
    <div class="row">
      <h1>Bookmarked Articles</h1>
      <% @bookmarked_articles.each do |article| %>
        <%= render "shared/articles-partial", article:article, bookmark:false%>
      <% end %>
    </div>
  </div>
