<!-- app/views/badges/show.html.erb -->
<div class="container home-container">
  <div class="row">
    <%= image_tag asset_path(@badge.image), alt: @badge.name, class: "card-img-top" if @badge.image.present? %>
    <h1><%= @badge.name %></h1>
    <p><%= @badge.description %></p>
  </div>
</div>
